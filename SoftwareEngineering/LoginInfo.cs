

namespace SoftwareEngineering
{
    // The 'LoginInfo' class is declared as a static class to store login-related information
    public static class LoginInfo
    {
        // The 'UserName' property holds the currently logged-in user's username
        public static string UserName;
    }
}

